[package]
name = "sv_matchit"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]
mlua = { version = "0.11.6", features = ["luajit", "module"] }

[profile.release]
lto = true
codegen-units = 1
